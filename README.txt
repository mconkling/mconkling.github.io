- edit in VS Code
- input the following into terminal

cd ~/Documents/portfolio
git status
git add --all
git commit -a -m"DESCRIBE CHANGES"
git push -u origin main

- input username and PAT when prompted